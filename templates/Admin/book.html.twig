{% extends 'base.html.twig' %}

{% block title %}
    <title>{{ book.title }}</title>
{% endblock %}

{% block main %}

    <article>

        <h1>{{ book.title }}</h1>

        <a class="btn" href="{{ path ('admin_delete_book',{'id':book.id })}}">Voulez-vous supprimer ce livre ? </a>

    </article>


{% endblock %}

{% block footer %}
    <footer> Footer de la page Book</footer>
{% endblock %}